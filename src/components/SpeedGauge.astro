---

---

<div class="animation-container">
    <svg viewBox="0 0 300 200" preserveAspectRatio="xMidYMid meet">
        <defs>
            <linearGradient
                id="gaugeGradient"
                x1="0%"
                y1="0%"
                x2="100%"
                y2="100%"
            >
                <stop offset="0%" style="stop-color:#f4a825;stop-opacity:1"
                ></stop>
                <stop offset="100%" style="stop-color:#f4a825;stop-opacity:0.6"
                ></stop>
            </linearGradient>
        </defs>

        <!-- Background gauge arc -->
        <path class="gauge-background" d="M 50 150 A 100 100 0 0 1 250 150"
        ></path>

        <!-- Filled gauge arc -->
        <path class="gauge-fill" d="M 50 150 A 100 100 0 0 1 250 150"></path>

        <!-- Needle -->
        <line class="gauge-needle" x1="150" y1="150" x2="150" y2="60"></line>

        <!-- Center circle -->
        <circle class="gauge-center" cx="150" cy="150" r="6"></circle>

        <!-- Labels -->
        <text class="gauge-label" x="50" y="175">Slow</text>
        <text class="gauge-label" x="250" y="175">Fast</text>

        <!-- Value display -->
        <text class="gauge-value" x="150" y="40">24/7</text>
    </svg>
</div>

<style>
    .animation-container {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    svg {
        width: 100%;
        height: 100%;
        filter: drop-shadow(0 0 20px rgba(244, 168, 37, 0.1));
        transition: filter 0.3s ease;
    }

    /* Use :global to ensure hover effects work when parent is hovered if needed, 
       but since the hover triggering element is outside (the card), we might need 
       to pass a prop or rely on the group-hover class if using Tailwind. 
       However, the original CSS used .card:hover .gauge-fill. 
       In Astro scoped styles, we can use :global(.group:hover) if the parent has 'group' class. 
       SuccessCard has 'group' class.
    */

    .gauge-background {
        fill: none;
        stroke: rgba(244, 168, 37, 0.15);
        stroke-width: 8;
        stroke-linecap: round;
    }

    .gauge-fill {
        fill: none;
        stroke: url(#gaugeGradient);
        stroke-width: 8;
        stroke-linecap: round;
        filter: drop-shadow(0 0 12px rgba(244, 168, 37, 0.4));
    }

    .gauge-needle {
        fill: none;
        stroke: #f4a825;
        stroke-width: 3;
        stroke-linecap: round;
        transform-origin: 150px 150px;
        filter: drop-shadow(0 0 8px rgba(244, 168, 37, 0.3));
    }

    .gauge-center {
        fill: #f4a825;
        filter: drop-shadow(0 0 12px rgba(244, 168, 37, 0.5));
    }

    .gauge-label {
        font-size: 11px;
        fill: #ece8df;
        opacity: 0.6;
        font-weight: 500;
        text-anchor: middle;
    }

    .gauge-value {
        font-size: 28px;
        font-weight: bold;
        fill: #f4a825;
        text-anchor: middle;
        filter: drop-shadow(0 0 8px rgba(244, 168, 37, 0.3));
    }

    /* Animaciones */
    @keyframes fillGauge {
        from {
            stroke-dasharray: 0 565;
            stroke-dashoffset: 0;
        }

        to {
            stroke-dasharray: 400 565;
            stroke-dashoffset: 0;
        }
    }

    @keyframes rotateNeedle {
        from {
            transform: rotate(-135deg);
        }

        to {
            transform: rotate(45deg);
        }
    }

    @keyframes pulse {
        0%,
        100% {
            r: 6;
            opacity: 1;
        }

        50% {
            r: 8;
            opacity: 0.7;
        }
    }

    @keyframes fillGaugeHover {
        from {
            stroke-dasharray: 400 565;
            stroke-dashoffset: 0;
        }

        to {
            stroke-dasharray: 450 565;
            stroke-dashoffset: 0;
        }
    }

    @keyframes rotateNeedleHover {
        from {
            transform: rotate(45deg);
        }

        to {
            transform: rotate(70deg);
        }
    }

    @keyframes pulseHover {
        0%,
        100% {
            r: 8;
            opacity: 1;
        }

        50% {
            r: 11;
            opacity: 0.8;
        }
    }

    .gauge-fill {
        animation: fillGauge 2.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
    }

    .gauge-needle {
        animation: rotateNeedle 2.5s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
    }

    .gauge-center {
        animation: pulse 2s ease-in-out infinite;
        animation-delay: 1.5s;
    }

    /* Hover effects triggered by parent group hover */
    :global(.group:hover) .gauge-fill {
        animation: fillGaugeHover 0.8s cubic-bezier(0.34, 1.56, 0.64, 1)
            forwards;
        filter: drop-shadow(0 0 20px rgba(244, 168, 37, 0.6));
    }

    :global(.group:hover) .gauge-needle {
        animation: rotateNeedleHover 0.8s cubic-bezier(0.34, 1.56, 0.64, 1)
            forwards;
    }

    :global(.group:hover) .gauge-center {
        animation: pulseHover 0.8s ease-in-out infinite;
    }

    :global(.group:hover) svg {
        filter: drop-shadow(0 0 30px rgba(244, 168, 37, 0.2));
    }
</style>
