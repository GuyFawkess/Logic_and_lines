# Sesión 2026-07-15 — Internacionalización (i18n)

## Cambios realizados

- Implementado `next-intl` con cookie-based locale (sin prefijos URL)
- Selector de idioma en Settings (English / Español)
- Persistencia: cookie `NEXT_LOCALE` + columna `profiles.language`
- Traducciones completas en `messages/en.json` y `messages/es.json`

### Áreas traducidas

| Área | Componentes |
|---|---|
| Settings | Página completa + formulario de idioma |
| Properties | Wizard (step-1, step-2, step-3), edit form, detail page, 16 componentes |
| Guests | Formulario, cards, detail page, scheduled messages |
| Dashboard | Página principal + métricas |
| Sidebar | Navegación completa |
| Calendar | Vistas y bloques |
| Messages | Lista y detalle |
| Plans | Panel de planes |

### Incidente de merge

El namespace `properties` se perdió en `messages/en.json` y `messages/es.json` por ediciones concurrentes. Reconstruido manualmente con 355 keys.

## Métricas de verificación

| Check | Resultado |
|---|---|
| Build | 39/39 páginas |
| Tests | 269/269 |
| TypeScript | 0 tsc errors |

## Bug detectado

El cambio de idioma a inglés desde el selector de Settings **no funciona** en prueba manual. Pendiente de arreglar en próxima sesión. Sospechas:
- Cookie `NEXT_LOCALE` no se persiste correctamente
- `i18n/request.ts` no lee la cookie actualizada
- `app/api/locale/route.ts` puede tener error de ruta/respuesta
- Posible hydration mismatch

## Pendientes

1. Traducir auth pages (`app/(auth)/*`)
2. Traducir landing page (`app/(marketing)/*`)
3. Arreglar bug de cambio de idioma a inglés